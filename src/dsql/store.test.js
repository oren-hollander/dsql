import ThrottlingStore from './store'
import DocumentServices from '../ds/documentServices'
import { createStore } from 'redux'
import Query, { APIQueryBuilder } from './query'

test('', () => {
  // let animationFrameCallback
  
  // const requestAnimationFrame = callback => {
  //   animationFrameCallback = callback
  // }

  // const reducer = (state = {}) => state

  // const store = createStore(reducer)

  // const ds = DocumentServices()
  // const dsAPI = APIQueryBuilder()

  // const throttlingStore = ThrottlingStore(requestAnimationFrame, store, ds)
  // throttlingStore.registerQuery({
  //   selectedComponent: dsAPI.getComponent('comp-1')
  // })

  // throttlingStore.dispatch({type: ''})
  // animationFrameCallback()
  // expect(store.getState().selectedComponent).toBe('comp-1')
})